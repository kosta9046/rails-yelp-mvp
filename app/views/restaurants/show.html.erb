<%= render @restaurant %>
<%= link_to "Create review", new_review_path(@restaurant) %>
<h2>Reviews:</h2>

<ul>
  <% @restaurant.reviews.each do |review| %>
    <li><%= review.content %>   ==><%= review.rating %></li>
  <% end %>

</ul>
